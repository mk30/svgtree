{
  "name": "svgtree",
  "version": "1.0.0",
  "description": "generate an svg tree in the browser",
  "main": "browser.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify browser.js -o bundle.js -dv"
    "watch": "watchify browser.js -o bundle.js -dv"
  },
  "author": "marina",
  "license": "MIT",
  "dependencies": {
    "virtual-dom": "^2.1.1",
    "virtual-hyperscript-svg": "^2.0.0",
    "browserify": "^13.0.0",
    "watchify": "^3.7.0"
  }
}
